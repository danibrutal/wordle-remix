## Wordle game made In Remix!

## Development (With Docker)

From your terminal:

```sh
docker compose up -d --build
```

This starts the app in development mode, rebuilding assets on file changes.

Access `http://localhost:3000/` to see the game
